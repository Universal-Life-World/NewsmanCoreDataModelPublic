import XCTest
@testable import NewsmanCoreDataModel

final class NewsmanCoreDataModelTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NewsmanCoreDataModel().text, "Hello, World!")
    }
}
